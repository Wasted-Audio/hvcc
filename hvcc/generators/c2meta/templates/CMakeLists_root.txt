cmake_minimum_required(VERSION 3.22)
include({{meta.sdk_path if meta.sdk_path is not none else '../'}}metamodule-plugin-sdk/plugin.cmake)

project({{ name }}
    VERSION {{ meta.version }}
    DESCRIPTION "{{ meta.description }}"
    LANGUAGES C CXX ASM
)

add_subdirectory(plugin)
